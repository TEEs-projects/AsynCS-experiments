# C4 deterministic state-scaling model

Contains the deterministic model contract, source evidence, calibration
summary, builder, validator, generated CSV outputs, and model notes. The
external Azure trace is referenced by its published URL and SHA-256; it is not
redistributed here.
