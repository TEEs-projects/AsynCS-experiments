# C1 accepted blocks

Includes the accepted 5x8 blocks for AsynCS, Native, and Reusable plus the
independent accepted 3x4 blocks. Child summaries and public-safe normalized
tables are retained; raw deployment logs and private prepared inputs are not.
The 3x4 source set remains independent from the 5x8 source set.
